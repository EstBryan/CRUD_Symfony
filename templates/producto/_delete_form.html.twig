<form method="post" action="{{ path('app_producto_delete', {'idProducto': producto.idProducto}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.idProducto) }}">
        
            <button id="boton"class="btn btn-danger btn-lg">Eliminar</button>

</form>
